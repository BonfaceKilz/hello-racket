#lang setup/infotab

(define name "hello-world")

(define blurb
  (list '(p "Hello world test package")))

(define primary-file "main.rkt")

(define categories '(net))

(define version "0.0.0.0")

(define deps '())

(define build-deps '())
